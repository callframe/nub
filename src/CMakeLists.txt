set(NUB_DIR "${NUB_SRCDIR}/nub")
add_subdirectory("${NUB_DIR}")

set(NUB_COMMON_DIR "${NUB_SRCDIR}/nub_common")
add_subdirectory("${NUB_COMMON_DIR}")

set(NUB_SOURCE_DIR "${NUB_SRCDIR}/nub_source")
add_subdirectory("${NUB_SOURCE_DIR}")
